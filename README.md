HGG22
hgg2003
Compartilhando tela
+1

Matheus Guerra — 08/06/2026 23:00
From retro-computing@yahoogroups.com  Wed Aug 28 15:22:10 2002
Return-Path: <retro-computing-return@yahoogroups.com>
Delivered-To: zzzz@localhost.netnoteinc.com
Received: from localhost (localhost [127.0.0.1])
    by phobos.labs.netnoteinc.com (Postfix) with ESMTP id AE23E43C34
    for <zzzz@localhost>; Wed, 28 Aug 2002 10:22:10 -0400 (EDT)

ham.txt
3 KB
From john.doe@enron.com  Tue Oct 23 09:45:12 2001
Return-Path: john.doe@enron.com
Delivered-To: zzzz@localhost.netnoteinc.com
Received: from localhost (localhost [127.0.0.1])
    by phobos.labs.netnoteinc.com (Postfix) with ESMTP id 8B90D43F99
    for <zzzz@localhost>; Tue, 23 Oct 2001 04:45:12 -0400 (EDT)
Received: from mailman.enron.com (mailman.enron.com [192.168.1.15]) by
    dogma.slashnull.org (8.11.6/8.11.6) with ESMTP id g7TFYfZ18351 for
    zzzz-enron@example.com; Tue, 23 Oct 2001 09:40:22 +0100
Message-Id: Pine.LNX.4.44.0110230938100.12345-100000@mailman.enron.com
From: "Doe, John" john.doe@enron.com
To: "Smith, Jane" jane.smith@enron.com, "Taylor, Michael" michael.taylor@enron.com
Cc: "Legal Department" legal@enron.com
Subject: Revised Q3 Financial Projections - CONFIDENTIAL
Date: Tue, 23 Oct 2001 09:38:15 -0500
MIME-Version: 1.0
Content-Type: text/plain; charset="us-ascii"
Content-Transfer-Encoding: 7bit
X-Mailer: Microsoft Outlook 8.5, Build 4.71.2173.0
X-Priority: 1 (Highest)

Jane, Mike,

Attached is the revised spreadsheet for the Q3 financial projections as we 
discussed in yesterday's meeting. 

I've adjusted the margins on the West Coast energy trades to reflect the 
new regulatory constraints. Please review the numbers in column H carefully, 
as they represent our most aggressive estimates.

Legal has already signed off on the compliance language for the final report, 
but I need your approval on these figures before I present them to the board 
on Thursday morning.

Let me know if you need to schedule a quick call this afternoon to go over 
the formulas.

Thanks,

John Doe
Senior Analyst, Risk Management
Enron Corp.
Ext. 4-5512
Matheus Guerra — 08/06/2026 23:20
https://mermaid.live/edit#pako:eNqNVO1q2zAUfRWhUWggCbYUJbE2CiXJj0BK6cf-bB5FteRYzLaCLLN-Ps1-7An2BH2xSXZsJ9nazviHr-655557j_AjjBQXkMK1ZpsEXM_DHNjn6AjMRSxz-fLr5acCXABRGJmqok5HKSsKCwAWEUmWglimKf0Qx8K7Rf3CaPVd2JBMIs_bhoMfkpuEos3dxwOOTBi2JYimYhwFLQGeTgWO3iWIWCbzRK1UvladEiZuWyKOUYziv4iaA86KhGnN7ikggLxCPyu1aegFtnS8pfeDyZijf-lst7koDOMKLOt91cdVoI5D-NXvA_d-A59u9Qk43gf3QtijlMqmtOW8PD07B4uri8-Ly_k5BbNaJ6iFHp-JNFHa-hapNGFgAC5K66Fzc9SrKWY3_ra555rXbXanbWDIwdA7sDNr442LWYf2molcEpwama8lZ0BkAINTe7V-i2I7nbsEh6PNl5eL5fXOZLXDdj31SLqUmZ1rcSeyTaqA32vLV_VkaLvWPckVyQ4SdUjvbSTurHoTWW3CHbFuva9vYvT2Jq41ywtZI4C9FAcuL7lwN2TnPoHB4OTJHwJWbKRm-snZ3PpdJdEQiNzY3BVLGXIA1DpdAfBOdWfrf4hqDDpwZF_YXu9VI27ViWt7rxphq05YV-07AG4AuAKMDpVXNsC-_b1JDqnRpejDTOiMuRA-uuoQmkRkIoTUfnIRszI1IQzzZ1u2YfkXpbKmUqtynUAas7SwUbnhzIi5ZPbf2UFEzoWeqTI3kJKKAdJHeAcpInhIPDRCIz-YYhIgm72H1J-MhyQggR8EY4Jt5rkPH6qe3pCQsT9BI-wRn6BpgJ__AOmBxUY
Online FlowChart & Diagrams Editor - Mermaid Live Editor
Simplify documentation and avoid heavy tools. Open source Visio Alternative. Commonly used for explaining your code! Mermaid is a simple markdown-like script language for generating charts from text via javascript.
graph TD
    %% Configuração de Estilo
    classDef targetNode fill:#f9d0c4,stroke:#e06666,stroke-width:3px,color:#000,font-weight:bold;
    classDef featureNode fill:#c4e3f9,stroke:#6fa8dc,stroke-width:2px,color:#000;
    classDef edgeLabel fill:#ffffff,color:#666666,font-size:11px,font-style:italic;

    %% Nó Raiz (Variável Dependente)
    C("Classe C <br/> {SPAM, HAM}"):::targetNode

    %% Nós Folhas (Features / Tokens Isolados)
    W1("w₁ (Token 1)<br/>ex: 'free'"):::featureNode
    W2("w₂ (Token 2)<br/>ex: 'win'"):::featureNode
    W3("w₃ (Token 3)<br/>ex: 'meeting'"):::featureNode
    Wn("wₙ (Token N)<br/>..."):::featureNode

    %% Probabilidades Condicionais (Arestas)
    C -- "P(w₁ | C)" --> W1
    C -- "P(w₂ | C)" --> W2
    C -- "P(w₃ | C)" --> W3
    C -- "P(wₙ | C)" --> Wn

    %% Legenda/Conceito Matemático
    subgraph Teorema de Bayes [Rede Bayesiana: Classificador Naive Bayes]
        direction BT
        C
        W1
        W2
        W3
        Wn
    end
Matheus Guerra — 08/06/2026 23:35
graph TD
    %% Definições de Estilo
    classDef implementado fill:#d4edda,stroke:#28a745,stroke-width:2px,color:#000;
    classDef planejado fill:#fff3cd,stroke:#ffc107,stroke-width:2px,color:#000,stroke-dasharray: 5 5;
    classDef db fill:#cce5ff,stroke:#007bff,stroke-width:2px,color:#000;
    classDef frontend fill:#e2e3e5,stroke:#6c757d,stroke-width:2px,color:#000;

    %% FASE 1: O que já existe
    subgraph FASE_ATUAL [FASE ATUAL: Implementada e Funcionando]
        direction TB
        A[/dataset/spam/*.txt<br/>(Arquivos Brutos)/]:::implementado
        B[ImportEmailService<br/>(Acionado pelo StartupRunner)]:::implementado
        C[EmailCleanerService<br/>(Pré-processamento NLP)]:::implementado
        D[EmailCleanerController<br/>(API REST de Teste)]:::implementado

        A -->|Lê arquivos| B
        B -->|Envia texto bruto| C
        D -->|Recebe POST /api/email-cleaner| C
    end

    %% BANCO DE DADOS
    subgraph DATABASE [Banco de Dados: PostgreSQL]
        direction TB
        DB1[(Tabela: emails<br/>content, is_spam)]:::db
        DB2[(Tabela: trained_models<br/>model_path, model_name)]:::db
    end

    C -->|Salva texto limpo e rótulo| DB1

    %% FASE 2: O que falta construir
    subgraph FASE_PLANEJADA [FASE PLANEJADA: ML e Integração (Futuro)]
        direction TB
        E[Tokenização + TF-IDF / Bag-of-Words<br/>(Vetorização)]:::planejado
        F[Treinamento do Modelo<br/>Naive Bayes, SVM, Random Forest - Smile]:::planejado
        G[API REST de Predição<br/>POST /api/classify]:::planejado
        H[Front-end Next.js<br/>(Interface do Usuário)]:::frontend

        E -->|Extrai features| F
    end

    %% Conexões entre as fases
    DB1 -.->|Fornece dados rotulados para treino| E
    F -.->|Salva caminho do modelo| DB2
    G -.->|Carrega modelo treinado| DB2
    G -.->|1. Envia texto novo para limpeza| C
    H -.->|2. Usuário cola e-mail| G
    G -.->|3. Retorna JSON {spam: true, prob: 0.97}| H
HGG22 [PGBR],  — 08/06/2026 23:41
dataset/spam + dataset/ham
        ↓
  ImportEmailService (StartupRunner)
        ↓
  EmailCleanerService
        ↓
  PostgreSQL: tabela emails (content, is_spam)
        ↓
  POST /api/model/train  ← acionado manualmente
        ↓
  NaiveBayesTrainingService
  (paginação 2000, parallelStream, ConcurrentHashMap)
        ↓
  PostgreSQL: word_frequencies + model_metadata
        ↓
  POST /api/classify  ← front-end Next.js
        ↓
  NaiveBayesClassifierService
  (Laplace + prior 0.5/0.5 + threshold 0.80)
        ↓
  JSON: { classe, probabilidade_spam, probabilidade_ham }
Matheus Guerra — 08/06/2026 23:53
graph TD
    %% Estilos
    classDef implementado fill:#d4edda,stroke:#28a745,stroke-width:2px,color:#000;
    classDef db fill:#cce5ff,stroke:#007bff,stroke-width:2px,color:#000;
    classDef frontend fill:#e2e3e5,stroke:#6c757d,stroke-width:2px,color:#000;
    classDef legado fill:#f8d7da,stroke:#dc3545,stroke-width:1px,color:#000,stroke-dasharray:3 3;

projeto.txt
8 KB
HGG22 [PGBR],  — 16:03
Fala amigo, quando tiver pronto, eu tô pronto.
Eu estava lanchando.
Matheus Guerra — 16:13
Bora?
HGG22
 iniciou uma chamada. — 16:13
Matheus Guerra — 16:30
docker info
docker compose up
Matheus Guerra — 16:47
modelo_treinado
HGG22 [PGBR],  — 16:48
Tipo de arquivo em anexo: acrobat
Redes Bayesianas - Estrutura de Rede Spam e Ham.pdf.pdf
623.29 KB
─────────────────────────────────────────────
Stage 1: build do JAR com Maven Wrapper
─────────────────────────────────────────────
FROM eclipse-temurin:21-jdk-alpine AS builder

WORKDIR /build

Copia somente os arquivos de dependência primeiro (cache de camadas)
COPY pom.xml ./
COPY .mvn .mvn
COPY mvnw ./
RUN chmod +x mvnw

Baixa dependências sem compilar (aproveita cache Docker)
RUN ./mvnw dependency:go-offline -q

Copia o código-fonte e compila
COPY src ./src
RUN ./mvnw package -DskipTests -q

─────────────────────────────────────────────
Stage 2: imagem de runtime mínima
─────────────────────────────────────────────
FROM eclipse-temurin:21-jre-alpine

RUN apk add --no-cache curl

WORKDIR /app

Copia apenas o JAR gerado
COPY --from=builder /build/target/*.jar app.jar

Porta exposta pelo Spring Boot
EXPOSE 8080

ENTRYPOINT ["java", "-jar", "app.jar"]
Matheus Guerra — 16:58
git reset HEAD~1
HGG22 [PGBR],  — 17:09
git lfs pull
Matheus Guerra — 17:37
modelo_treinado.sql filter=lfs diff=lfs merge=lfs -text
# 📚 Projetos

Repositório com o projeto final de inteligencia computacional.

---

message.txt
6 KB
﻿
# 📚 Projetos

Repositório com o projeto final de inteligencia computacional.

---

## 🛠️ Tecnologias Utilizadas

* **REACT** — estrutura e estilização das interfaces
* **Java** - Lógica da Aplicação.
* **Vercel** — deploy e hospedagem dos projetos

---

## 🚀 Projetos

### 📧 Detector de Spam

Classificador automático de e-mails spam/ham utilizando técnicas de aprendizado de máquina e processamento de linguagem natural. O pipeline completo inclui pré-processamento de texto, extração de features com TF-IDF e comparação entre múltiplos modelos (Naive Bayes, SVM, MLP, Random Forest).

* 🧠 **Disciplina:** Inteligência Computacional

---

## 📌 Organização do Repositório

```
📦 repositorio
 ┣ 📂 Projeto de Spam
 ┃ ┣ 📂 Front-End
```

---

## Executar com Docker

O projeto inclui o banco **já treinado** (`modelo_treinado.sql`, ~1 GB). Na primeira subida, o PostgreSQL restaura esse dump automaticamente — **não é necessário treinar o modelo**.

### Pré-requisitos

- Docker Desktop ou Docker Engine + Compose v2
- Arquivo `modelo_treinado.sql` na raiz do projeto (via Git LFS ou download externo)
- [Git LFS](https://git-lfs.com/) instalado (se clonar o repositório)

### Obter o banco pré-treinado

**Opção A — Git LFS (recomendado):**

```bash
git lfs install
git clone <url-do-repositorio>
cd ProjetoSpam
git lfs pull
```

**Opção B — Link externo:** baixe `modelo_treinado.sql` (Google Drive etc.) e coloque na raiz do projeto, ao lado de `docker-compose.yml`.

### Subir tudo de uma vez

```bash
docker compose up --build
```

Ordem de subida: **PostgreSQL** (restore do dump) → **backend** → **front-end**.

Na **primeira execução**, aguarde nos logs do container `db` a mensagem `Restore concluído`. O restore de ~1 GB pode levar **15 a 60 minutos**, conforme o hardware.

### URLs após subida

| Serviço | URL |
|---------|-----|
| Front-end | http://localhost:3000 |
| Backend API | http://localhost:8080 |
| PostgreSQL (host) | localhost:5433 |

### Testar classificação (sem treinar)

1. Abra http://localhost:3000
2. Cole um e-mail ou envie um arquivo `.txt`
3. O sistema classifica usando o modelo já restaurado no banco

Teste via API:

```bash
curl -X POST http://localhost:8080/api/classify \
  -H "Content-Type: application/json" \
  -d "{\"text\":\"ganhe prêmio grátis clique aqui\"}"
```

### Comandos úteis

```bash
docker compose down              # parar os containers
docker compose down -v           # parar e apagar volume (força novo restore na próxima subida)
docker compose logs -f db        # acompanhar restore do banco
docker compose logs -f backend
curl http://localhost:8080/actuator/health
```

### Observações Docker

- **Não** é necessário pasta `dataset/` nem `POST /api/model/train` para avaliar o projeto.
- O restore roda **somente** na primeira criação do volume `pgdata`. Subidas seguintes reutilizam o banco persistido.
- Se o banco não foi restaurado (volume antigo vazio), execute `docker compose down -v` e suba novamente.
- Para desenvolvimento local com importação manual do corpus, use `APP_IMPORT_ENABLED=true` e monte `./dataset` (ver seção de treinamento abaixo).

---

## Treinamento do modelo (Naive Bayes)

O treinamento é **assíncrono** e otimizado para grandes volumes (~630k e-mails): leitura por cursor (sem OFFSET), limpeza de texto, INSERT em bulk via JDBC.

### Endpoints

| Método | URL | Descrição |
|--------|-----|-----------|
| `POST` | `http://localhost:8080/api/model/train` | Inicia treino; retorna **202** com `jobId` |
| `GET` | `http://localhost:8080/api/model/train/status/{jobId}` | Progresso (`progressPercent`) e sumário ao concluir |
| `GET` | `http://localhost:8080/api/model/train/latest` | Último job disparado |

### Fluxo recomendado

1. Subir o backend (`.\mvnw.cmd spring-boot:run`) com PostgreSQL em `spamdetector`.
2. `POST /api/model/train` — anotar o `jobId` da resposta.
3. Acompanhar logs no terminal ou `GET .../status/{jobId}` a cada 1–2 min.
4. Quando `status` = `COMPLETED`, validar no banco:
   - `SELECT COUNT(*) FROM word_frequencies;`
   - `SELECT * FROM model_metadata ORDER BY trained_at DESC LIMIT 1;`

### Configuração (`application.properties`)

| Propriedade | Padrão | Descrição |
|-------------|--------|-----------|
| `training.batch-size` | `5000` | E-mails por lote na leitura |
| `training.insert-batch-size` | `1000` | Linhas por batch JDBC |
| `training.max-emails` | `0` | `0` = todos; use `10000` ou `50000` para teste rápido |

### Observações

- Apenas **um** treinamento por vez (segundo `POST` retorna 409).
- Jobs ficam em memória; ao reiniciar o servidor, use os logs ou o banco para confirmar o último treino concluído.
- Tempo estimado com ~630k e-mails: **20–50 min** (hardware e tamanho médio do `content` variam).